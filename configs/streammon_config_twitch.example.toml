[streammon]
# Where downloaded Twitch streams are saved.
working_directory = "download_twitch"

[twitch-dlp]
# Arguments passed to twitch-dlp.
args = [
    "--live-from-start",
    "--retry-streams", "60",
    "--output", "[%(upload_date)s] [%(id)s] [%(uploader)s] [%(title)s].%(ext)s"
]

[scraper]
# How often to check the full channel list.
poll_interval = "30s"

# Rate limit for channel checks.
max_requests_per_second = 2

# Stop a stalled --live-from-start recovery after this many failed playlist attempts and fall back to live-edge capture. Set to 0 to disable.
download_wait_retries = 3

# ========================== Channels ========================== #
# Use filters = [".*"] to download every live stream.
# Optional additional_args are appended to the global twitch-dlp args.
# Twitch-only: use "--strip-live-from-start" for channels that do not preserve VODs.

# Example:
# [[channel]]
# id = "sayasairroxs"
# name = "Saya Sairroxs"
# filters = [".*"]
# additional_args = ["--strip-live-from-start"]
